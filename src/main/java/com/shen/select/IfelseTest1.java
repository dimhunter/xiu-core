package com.shen.select;

/**
 * if ... else ...
 * 
 * else是可选的。不一定必须有else。
 * 
 * @author shen
 *
 */
public class IfelseTest1 {
	
	// if(condition) statement1 ;
	// else statement2;
	
	// 跟在if或else语句后的语句如果只有一句，就是一个分号的话，可以省去大括号。
	// 如果多的话，是语句块，就要带上大括号。
}
